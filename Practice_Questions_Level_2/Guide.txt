C Practice Questions Level 2:

''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Note*: For folders with multiple files, I have made the code in seperate files as follows:
1. main.c - holds the main program.
2. header.h - holds the function declarations used in the program.
3. functions.c - holds the functions used for the main program.
4. functions.o - this is just the object file created of the functions.c

To run the program compile the main file along with object file of functions as shown below:
"gcc -Wall -Wextra <mainprogramfilename>.c <functionfilename>.o -o <outputfilename>"

Programs without multiple functions does not have seperate files holding functions or headers. The names of files are different according to the session name and question number.
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''

1. Take a digit/number from user and print the pattern.
	Ex: n = 4

	Expected output:
	*
	**
	***
	****

2. Similar to assignment one.
	Ex: n=4
	********
	***  ***
	**    **
	*      *
	**    **
        ***  ***
        ********


	    *
           * *
          * * *
         * * * *
          * * *
           * *
            *

3. Write a menu based C program with following options.
	a. Find a number is Armstrong number or not.
	b. Print range of Armstrong numbers.
	c. Exit.


4. Write a menu based C program with following options.
	a. Find a number is Shakuntala Devi's number or not.
	b. Print range of Shakuntala Devi's numbers.
	c. Exit. 


5. Implement C program with following menu-choices:
	a. Reverse a string.
	b. Check if string is palindrome.
	c. Compare two strings.
	d. String copy (Hint: strcpy)
	e. String-tokenizer. ( ex strtok)
	f. String Concatenate. ( Hint: strcat)
	
6. Implement following on expressions:
	a. Infix to prefix.
	b. prefix to postfix.
	c. prefix to infix.
	d. postfix to prefix.
	e. postfix to infix.

7. Implement following using command line arguments :
	Ex: myEx Welcome
	Output:
		W e l c o m e
                e           m
                l           o
                c           c
                o           l
                m           e
                e m o c l e W

8. Implement following Queue's using arrays :
	a. Regular Queue.
	b. Circular Queue.
	c. Priority Queue.
	d. Double Ended Queue.
 
